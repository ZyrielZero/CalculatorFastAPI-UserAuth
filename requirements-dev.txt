# Test and lint tooling. Never installed in the Docker image: keeping these
# out of the runtime freeze removes requests/urllib3/playwright from the
# Trivy scan surface entirely.
-r requirements.txt

pytest==8.3.3
pytest-cov==6.0.0
httpx==0.27.2
requests==2.32.3
# playwright >=1.50.0 required: 1.48/1.49 hard-pin greenlet==3.1.1, which
# conflicts with the greenlet==3.5.3 SQLAlchemy resolves in requirements.txt.
playwright==1.50.0
pylint==3.3.1
pytest-pylint==0.21.0